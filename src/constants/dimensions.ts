const _WIDTH_PERCENTAGE = 0.9;
const _CANVAS_HEIGHT_PERCENT = 0.8;
export const VIDEO_WIDTH = window.innerWidth * _WIDTH_PERCENTAGE;
export const VIDEO_HEIGHT = window.innerHeight;
export const CANVAS_HEIGHT = VIDEO_HEIGHT * _CANVAS_HEIGHT_PERCENT;
